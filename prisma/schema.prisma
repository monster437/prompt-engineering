generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model ProviderConfig {
  id           String   @id @default(cuid())
  type         ConfigType
  providerName String
  baseUrl      String
  apiKey       String
  modelsJson   String
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

model Workspace {
  id                 String   @id @default(cuid())
  title              String
  mode               WorkspaceMode
  outputLanguage     OutputLanguage
  selectedTextModel  String?
  selectedTextConfig String?
  selectedTargetType String
  selectedImageConfig String?
  selectedImageAspectRatio String @default("auto")
  selectedImageModel String?
  sourcePrompt       String   @default("")
  sourcePromptImages String   @default("[]")
  questionMessages   String   @default("[]")
  answers            String   @default("[]")
  finalPrompt        String?
  parameterSummary   String?
  refineInstruction  String?
  generatedImageResult String?
  status             WorkspaceStatus @default(IDLE)
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt
}

model ReverseWorkspace {
  id                 String   @id @default(cuid())
  title              String
  selectedTextConfig String?
  selectedTextModel  String?
  outputLanguage     OutputLanguage
  userInstruction    String   @default("")
  sourcePromptImages String   @default("[]")
  resultJson         String?
  errorMessage       String?
  status             ReverseWorkspaceStatus @default(IDLE)
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt
}

enum ConfigType {
  TEXT
  IMAGE
}

enum WorkspaceMode {
  INTERVIEW
  OPTIMIZE
}

enum OutputLanguage {
  ZH
  EN
}

enum WorkspaceStatus {
  IDLE
  ASKING
  GENERATING
  REFINING
  ERROR
}

enum ReverseWorkspaceStatus {
  IDLE
  GENERATING
  COMPLETED
  ERROR
}
